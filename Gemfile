source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in ruby-primes-using-rust.gemspec
gemspec

group :development, :test do
  gem "rspec", "~> 3.0"
  gem 'rubocop', require: false
end
